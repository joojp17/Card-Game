{
  "name": "cards-against-jewels",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @cards-against-jewels/server",
    "dev:web": "npm run dev --workspace @cards-against-jewels/web",
    "dev:server": "npm run build --workspace @cards-against-jewels/shared && npm run dev --workspace @cards-against-jewels/server",
    "build": "npm run build --workspaces",
    "test": "npm run build --workspace @cards-against-jewels/shared && npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=22"
  }
}
